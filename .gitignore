# Godot
.godot/
/out/
export_presets.cfg.bak

# Downloaded toolchain (never commit the engine/templates)
godot
*.tpz
*.tmp

# Verification / preview tooling (never committed)
/verify/
/playbooks/

# Binary game assets — fetched/generated by scripts/fetch_assets.sh (see README "Building").
# The bulk of the world (palms, docks, crowd) streams from R2 at runtime; the Meshy player and
# townsfolk are hosted on R2 too.
/models/*.glb
/models/Textures/
/audio/*.wav
/audio/*.ogg
*.import
